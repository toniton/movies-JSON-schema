{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "ShowTime",
  "description": "A time where a specific movie is available in a specific location of a cinema",
  "type": "object",
  "properties": {
    "id": {
      "description": "Unique identifier for the showtime",
      "type": "integer"
    },
    "cinema_id": {
      "description": "Cinema where this showtime is available",
      "type": "integer"
    },
    "location_id": {
      "description": "Location where this showtime is available",
      "type": "integer"
    },
    "movie_id": {
      "description": "Movie that would be showing at this showtime",
      "type": "integer"
    },
    "time": {
      "description": "The date/time when this movie would be showing",
      "type": "string"
    }
  },
  "required": [
    "id",
    "cinema_id",
    "location_id",
    "movie_id",
    "time"
  ]
}