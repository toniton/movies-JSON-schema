{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ShowTime",
  "description": "A time where a specific movie is available in a specific location of a cinema",
  "type": "object",
  "properties": {
    "id": {
      "description": "Unique identifier for the showtime",
      "type": "integer"
    },
    "cinema": {
      "description": "Cinema where this showtime is available",
      "type": "integer"
    },
    "movie": {
      "description": "Movie that would be showing at this showtime",
      "type": "integer"
    },
    "weekday": {
      "type": "string",
      "description": "The weekday which this showtime airs"
    },
    "time": {
      "description": "The time when this showtime airs",
      "type": "string"
    },
    "date": {
      "description": "The date when this movie would be showing",
      "type": "string"
    }
  },
  "required": [
    "id",
    "cinema",
    "movie",
    "day",
    "time"
  ]
}
